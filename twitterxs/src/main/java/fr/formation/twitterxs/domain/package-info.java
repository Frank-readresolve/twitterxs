/**
 * Root package for domain classes.
 * <p>
 * Packages are structured as follow:
 * <ul>
 * <li>Root package contains utility classes and custom implementation such as
 * converters
 * <li>{@code entities} subpackage contains mapped business entities
 * <li>{@code repositories} subpackage contains custom repositories to deal with
 * business entities
 * <li>{@code jparepositories} subpackage contains JPA repositories interfaces
 * to deal with business entities when simple CRUD operations are needed
 * </ul>
 */
package fr.formation.twitterxs.domain;